import XCTest
@testable import SwiftLambdaProxy

final class SwiftLambdaProxyTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftLambdaProxy().text, "Hello, World!")
    }
}
